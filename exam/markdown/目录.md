# Software Tools 按 Week 目录

## 1. 课程笔记（`index777.pdf`，按 Week 分页）

- Week 01（HTTP）`PDF p.1-16`
  - `p.4` 请求消息 vs 响应消息（结构与区别）
  - `p.9` HTTP Status Codes 五大类（对应模拟题 Q3）
  - `p.9` `fetch` 的 Promise：有 HTTP 响应（含 404/500）=> fulfilled；网络失败才 rejected
  - 对应真题：`tb2 Q1 Q2 Q3 Q22`  
    去这里找：`index777 p.2-3`（HTTP 请求/响应与元数据）、`p.9`（状态码）、`p.1-2`（OSI/TCP-IP）
- Week 02（HTML）`PDF p.17-28`
  - `p.20` HTML elements 基础（元素识别与语义）
  - 对应真题：`tb2 Q4 Q5 Q6`  
    去这里找：`index777 p.17-21`（HTML 元素、语义化标签、绝对/相对 URL）
- Week 03（CSS）`PDF p.29-46`
  - `p.32` 选择器基础：`#jumble`、`.jumble`、后代（空格）、子代（`>`）
  - `p.33` 组合器速记：后代、子代、相邻兄弟（`+`）、通用兄弟（`~`）
  - `p.40` Box model 与 margin touching（margin collapse）
  - `p.40` Media Query 做题套路（print/screen、继承与覆盖）
  - 对应真题：`tb2 Q7 Q8 Q9 Q10 Q11`  
    去这里找：`index777 p.31-33`（选择器/组合器）、`p.39-40`（盒模型、margin collapse、media query）
- Week 04（CSS 进阶 / 布局）`PDF p.47-55`
  - `p.50` Grid layout 与 auto-placement（按 writing mode 放置）
  - 对应真题：`tb2 Q12`  
    去这里找：`index777 p.50-52`（Grid 自动放置、布局规则）
- Week 05（JavaScript）`PDF p.56-64`
  - `p.56` 字符串拼接陷阱：字符串 + 数字 => 拼接，不是数值加法
  - 对应真题：`tb2 Q13 Q14 Q15`  
    去这里找：`index777 p.56-60`（类型转换、关键字、`let` 作用域/重声明）
- Week 06（未在当前文件中检出独立 Week 标题）`-`
- Week 07（Asynchronous JavaScript）`PDF p.65-69`
  - `p.67` `Promise.all([...]).then(...)`：全部 fulfilled 才触发一次；任一 rejected 则整体 rejected
  - 对应真题：`tb2 Q16 Q17`  
    去这里找：`index777 p.66-67`（`fetch` Promise 状态、`Promise.all` 触发条件）
- Week 08（Web Scraping）`PDF p.70-78`
  - `p.71` `wget` 行为对比：默认 / `-nc` / `-N`
  - 服务器端校验（server-side validation）是安全最终防线；前端校验主要提升体验
  - 对应真题：`tb2 Q18 Q19 Q20 Q21`  
    去这里找：`index777 p.70-73`（表单校验、wget、抓取规范、BeautifulSoup）
- Week 09（密码学 / PGP / OpenSSL）`PDF p.79-89`
  - 证书链阅读：`s`(subject)、`i`(issuer)、`NotAfter`(过期时间)
  - 邮件签名 + 加密：保密性 + 可验证发送者身份/完整性
  - 对应真题：`tb2 Q23 Q24 Q25 Q28 Q32`  
    去这里找：`index777 p.79-85`（公钥密码学、证书链、邮件签名/加密、PGP）
- Week 10（Protocols, Sockets, Inetd）`PDF p.90-96`
  - `p.90` RFC 与 IETF（标准文档与标准组织）
  - 对应真题：`tb2 Q26 Q27`  
    去这里找：`index777 p.90-94`（RFC/IETF、Socket、协议与端口）
- Week 11（Testing）`PDF p.97-103`
  - `p.101` Testing 总结（测试目标与常见题型）
  - 对应真题：`tb2 Q29 Q30 Q31`  
    去这里找：`index777 p.97-103`（fuzz testing、property-based testing、QuickCheck 性质）

---



